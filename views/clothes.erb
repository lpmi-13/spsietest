<!DOCTYPE HTML>
<head>
<link rel="stylesheet" href="//code.jquery.com/ui/1.11.3/themes/smoothness/jquery-ui.css">
  <script src="//code.jquery.com/jquery-1.10.2.js"></script>
  <script src="//code.jquery.com/ui/1.11.3/jquery-ui.js"></script>
  <script src="/jquery.ui.touch-punch.min.js"></script>
  <link rel="stylesheet" href="/custom.css">
  <style>
  #sortable { list-style-type: none; margin: 0; padding: 0; width: 100%; text-align: center; overflow: auto;}
  #sortable li { margin: 10px 15px 10px 15px; padding: 0.6em; padding-left: 2.5em; font-size: 2.4em; height: auto; overflow: auto;}
  #sortable li span { position: absolute; margin-left: -1.8em; }
  </style>
<script>
$.fn.randomize = function(selector){
    var $elems = selector ? $(this).find(selector) : $(this).children(),
        $parents = $elems.parent();

    $parents.each(function(){
        $(this).children(selector).sort(function(){
            return Math.round(Math.random()) - 0.5;
        }).detach().appendTo(this);
    });

    return this;
};



$(function() {
	$("#sortable").sortable();
	$("#sortable").disableSelection();

  $('ul#sortable').randomize('li');

	$('ul#sortable').sortable({
           update: function(event, ui) {
            var sortArray = $("#sortable").sortable("toArray");
            var eva = sortArray.indexOf("eva");
            var imp = sortArray.indexOf("imp");
            var sol = sortArray.indexOf("sol");
            var pro = sortArray.indexOf("pro");
            var sit = sortArray.indexOf("sit");
            console.log(sortArray);
            console.log(eva, imp, sol, pro, sit);
              $("#eva").toggleClass("fifth", eva == 4);
              $("#imp").toggleClass("fourth", imp == 3);
              $("#sol").toggleClass("third", sol == 2);
              $("#pro").toggleClass("second", pro == 1);
              $("#sit").toggleClass("first", sit == 0);
            if (eva == 4 && imp == 3 && sol == 2 && pro == 1 && sit == 0){
              $("ul#sortable").sortable("disable");
              $("h1").replaceWith("<h1>Good job, you have put the statements in the correct order! All done!</h1>");
            }     
        }
    });
});

</script>
</head>
<body>

<h1>Put these statements in order.
<br>Remember the <b>"Situation => Problem => Solution => Implications => Evaluation"</b> format.<br>
(hint: the statements will turn colours when they are in the correct position)</h1>

<ul class="target" id="sortable">
	<li class="ui-state-default" id="sit"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>I have recently moved the the UK, and it is much colder here than it was in my home country. I find that I am constantly wearing more clothing than I am used to</li>
	<li class="ui-state-default" id="pro"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>When I wear so much clothing, I feel like people think I am out of shape. It makes me sad that people will think I am less fit than I actually am</li>
	<li class="ui-state-default" id="sol"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>I will buy new clothes that are made of heavier material, so that I don't have to wear so many layers. This way, I will only need one jumper, so long as it is made of sturdy material</li>
	<li class="ui-state-default" id="imp"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>Since prices for clothing in the UK are very expensive, and I don't currently have enough money to afford buying these new clothes, I will have to ask my family for a short-term loan</li>
	<li class="ui-state-default" id="eva"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span>My family is always very happy to support me, and I'm sure they will see this as an investment in my future, so it's definitely a good plan for all parties concerned</li>
</ul>

</body>
</html>